# Generate language files

This document describes how to generate language files for the web interface.

The following command will update .pot, .mo, .po for JS scripts, and JSON language files.

```bash
composer gen:lang
```
