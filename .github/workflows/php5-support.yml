name: PHP v5 Support

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  php5_check:
    uses: publishpress/github-workfows/.github/workflows/php-version-check.yml@main
    with:
      expected_php: "5.6"
      php_version_check: "70000"
